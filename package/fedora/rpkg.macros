function git_commit_no {
	commits=$(curl -s 'https://api.github.com/repos/montylab3d/eigen/compare/main...fedorawork' | grep "ahead_by" | sed -s 's/ //g' | sed -s 's/"ahead_by"://' | sed -s 's/,//')
	echo -n $((commits + 1))
}
